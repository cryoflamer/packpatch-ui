# packpatch-ui

Desktop UI for the PackPatch workflow: select project files, create disposable git repository packs, and apply generated patches with validation.

## Development

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
python -m packpatch_ui
```
